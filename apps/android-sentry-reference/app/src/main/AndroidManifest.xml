<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pass -PsentryDsn=https://PUBLIC_KEY@HOST/PROJECT_ID when building. -->
        <meta-data
            android:name="io.sentry.dsn"
            android:value="${sentryDsn}" />
        <meta-data
            android:name="io.sentry.debug"
            android:value="true" />
        <meta-data
            android:name="io.sentry.traces.sample-rate"
            android:value="1.0" />
        <meta-data
            android:name="io.sentry.traces.user-interaction.enable"
            android:value="true" />
        <meta-data
            android:name="io.sentry.logs.enabled"
            android:value="true" />
        <meta-data
            android:name="io.sentry.send-default-pii"
            android:value="false" />
        <!-- Privacy-safe training defaults: opt in only after a data review. -->
        <meta-data
            android:name="io.sentry.attach-screenshot"
            android:value="false" />
        <meta-data
            android:name="io.sentry.attach-view-hierarchy"
            android:value="false" />
    </application>

</manifest>
